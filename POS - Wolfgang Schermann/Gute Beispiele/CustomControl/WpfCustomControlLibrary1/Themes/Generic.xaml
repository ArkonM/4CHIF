<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:WpfCustomControlLibrary1">
    <Style TargetType="{x:Type local:CustomControl1}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CustomControl1}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">

                        <Grid>
                            <Button Content="Change" HorizontalAlignment="Left" Margin="679,43,0,0" VerticalAlignment="Top" Width="75" x:Name="btn"/>
                            <Grid Visibility="Visible" Margin="78,118,211,35" x:Name="grid1">
                                <Label Content="Hallo" Width="50" Height="50"></Label>
                            </Grid>
                            <Grid Visibility="Collapsed" Margin="78,118,211,35" x:Name="grid2">
                                <Label Content="Tschau" Width="50" Height="50"></Label>
                            </Grid>
                        </Grid>

                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
